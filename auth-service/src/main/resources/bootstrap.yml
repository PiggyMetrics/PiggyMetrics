spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://10.109.120.155:8888
#      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user

---
spring.profiles: docker

spring:
  cloud:
    config:
      uri: http://pubconfig.pubconfig--dev-space:80
